<%= form_for([:admin, @institution]) do |f| %>
  <% if @institution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@institution.errors.count, "error") %> prohibited this institution from being saved:</h2>

      <ul>
      <% @institution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :style => "width:600px;" %>
  </div>
  
  <div class="field">
  	<%= f.label :information %><br>
  	<%= f.text_area :info, :style => "width:600px;height:150px;" %>
  </div>
  
  <div class="field">
  	<%= f.label :link %><br>
  	<%= f.text_field :url, :style => "width:600px;" %>
  </div>
  
  <div class="field">
  	<%= f.label :address %><br>
  	<%= f.text_area :address, :style => "width:400px;height:150px;" %>
  </div>
  
  <div class="field">
  	<%= f.label :contact %><br>
  	<%= f.text_area :contact, :style => "width:400px;height:150px;" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
